def calculate_gradients(dataset, batch_size, start_index, network_structure, weights):
  # TODO Calcular e retornar gradientes para 1 mini-batch. start_index é o índice da primeira instância
  # do mini-batch.
  return None

def backpropagation(dataset, network_structure, initial_weights, learning_rate, momentum, batch_size):
  # TODO Usar a calculate_gradients repetidamente para treinar a rede neural em mini-batches, até atingir um
  # critério de parada.
  return None